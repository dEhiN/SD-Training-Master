// DOM event listener

// loads the moment the HTML is loaded on the browser
document.addEventListener("DOMContentLoaded", function () {
    console.log("HTML has entirely loaded");
});

// on load method runs after all Image, css and content of the website are loaded on users browser
window.onload = function () {
    console.log("Website has entirely loaded");
}